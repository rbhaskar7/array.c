#include<stdio.h>
int main()
{
  int a[20],i,n;
  printf("Enter number of elements:");
  scanf("%d",&n);
  printf("Enter your array");

  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }

  printf("Your entered array is:");
  for(i=0;i<n;i++)
  {
    printf("%d",a[i]);
  }
  return 0;
}
